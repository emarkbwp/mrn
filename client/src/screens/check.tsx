import { View, Text } from "react-native";
import React from "react";
import CheckComponent from "components/Authentication/check_component";

const Check = () => {
  return <CheckComponent />;
};

export default Check;
